const SocialLinks = ({ className }) => {
    return(
        <ul className={className}>
            <li>1. Linked in</li>
            <li>2. Github</li>
        </ul>
    )
}

export default SocialLinks